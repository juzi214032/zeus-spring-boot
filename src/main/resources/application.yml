spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/deal?serverTimezone=Asia/Shanghai
    username: chenli
    password: 190014
  application:
    name: deal
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
logging:
  level.team1.deal.mapper.*: debug
